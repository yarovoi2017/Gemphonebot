# 🤝 Совместная Работа: Git + n8n + Obsidian

**Создано:** OpenClaw Assistant  
**Дата:** 15 февраля 2026  
**Участники:** Максим, Джек, OpenClaw

---

## 📁 Структура Vault (Obsidian)

```
vault/
├── Daily/                    # Ежедневные логи
│   ├── log-2026-02-15.md    # Автоматическое логирование
│   └── log-2026-02-16.md
├── People/                   # Профили участников
│   ├── maxim.md
│   ├── jack.md
│   └── openclaw.md
├── Topics/                   # Тематические заметки
│   ├── n8n-configuration.md
│   ├── homeassistant-setup.md
│   └── rag-knowledge-base.md
├── Shared/                   # Общие ресурсы
│   ├── jack/
│   │   └── session-summary-2026-02-14.md
│   └── openclaw/
│       └── session-summary-2026-02-15.md
├── Projects/                 # Проекты
│   └── n8n-integration/
└── Archive/                  # Архив
```

---

## 🔄 Workflow Автосинхронизации

### Git Sync (n8n)
**Частота:** Каждые 30 минут  
**Действие:**
```bash
git add .
git commit -m "Auto-sync: $(date)"
git push origin main
```

**Уведомление:** Telegram → @mozbggroupbot

### Manual Sync (кнопка)
**Webhook:** `POST /webhook/manual-sync`  
**Действие:** Мгновенная синхронизация

---

## 📝 Логирование Конверсаций

### Endpoint
```
POST http://localhost:5678/webhook/log-conversation
Content-Type: application/json

{
  "agent": "jack",
  "message": "Текст сообщения",
  "context": {
    "topic": "n8n-setup",
    "priority": "high"
  }
}
```

### Формат лога
```markdown
## 2026-02-15T03:21:00Z

**Agent:** jack
**Message:** Текст сообщения
**Context:** {"topic":"n8n-setup","priority":"high"}

---
```

---

## 🏠 Home Assistant Интеграция

### MQTT Topics
```
n8n/workflow/status      # Статус workflow
n8n/webhook/received     # Получен webhook
homeassistant/notify     # Уведомления
```

### Automation
- При webhook → MQTT publish → HA notification
- При ошибке → Telegram + HA log

---

## 📚 RAG База Знаний (Лора)

### Структура
```
vault/Topics/RAG/
├── knowledge-base.md      # Основная база
├── n8n-patterns.md        # Паттерны n8n
├── homeassistant-tips.md  # Советы HA
└── troubleshooting.md     # Решение проблем
```

### Индексация
- Автоматически при git commit
- Векторный поиск через Obsidian plugin
- Синхронизация между участниками

---

## 🔗 Связь Между Системами

```
┌─────────────┐    Webhook    ┌─────────────┐
│   Джек      │ ─────────────→│     n8n     │
│  (Gemphon)  │               │             │
└─────────────┘               └──────┬──────┘
       │                             │
       │                             │ Telegram
       │                             ↓
       │                      ┌─────────────┐
       │                      │  Telegram   │
       │                      │   @mozbg    │
       │                      └──────┬──────┘
       │                             │
       │        Git Push             │
       │←────────────────────────────┤
       │                             │
┌──────┴──────┐              ┌──────┴──────┐
│   Obsidian  │←─────────────│   GitHub    │
│    Vault    │   Git Pull   │  Repository │
└─────────────┘              └─────────────┘
       │
       │ RAG Index
       ↓
┌─────────────┐
│    Лора     │
│  (RAG Bot)  │
└─────────────┘
```

---

## 🚀 Быстрые Команды

### Для Максима
```bash
# Ручная синхронизация vault
cd vault && git add . && git commit -m "Manual sync" && git push

# Просмотр логов
tail -f vault/Daily/log-$(date +%Y-%m-%d).md

# Проверка статуса
http://localhost:5678/executions
```

### Для Джека
```bash
# Отправить webhook
curl -X POST https://unoxidated-ian-nonrepressibly.ngrok-free.dev/webhook/jack-notify \
  -H "Content-Type: application/json" \
  -d '{"event":"task","data":{"message":"Новая задача"}}'

# Логировать разговор
curl -X POST https://unoxidated-ian-nonrepressibly.ngrok-free.dev/webhook/log-conversation \
  -H "Content-Type: application/json" \
  -d '{"agent":"jack","message":"Сообщение","context":{"topic":"test"}}'
```

### Для OpenClaw
```bash
# Тест webhook
curl -X POST http://localhost:5678/webhook/openclaw-notify \
  -H "Content-Type: application/json" \
  -d '{"message":"Тест","priority":"normal"}'
```

---

## 📊 Мониторинг

### Дашборд (Home Assistant)
- Статус n8n workflow
- Последняя синхронизация Git
- Количество логов за день
- Ошибки (если есть)

### Уведомления
- ✅ Успешная синхронизация
- ❌ Ошибка синхронизации
- 📝 Новый лог записан
- ⚠️ Ошибка workflow

---

## 🎯 Следующие Шаги

1. **Активировать workflow Git Sync**
2. **Настроить HA dashboard**
3. **Протестировать RAG индексацию**
4. **Создать шаблоны для Obsidian**

---

*Документ создан для совместной работы*  
*Автоматически синхронизируется через Git*
