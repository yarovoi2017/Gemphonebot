# Git Sync Guide

## Быстрый старт

### Инициализация
```bash
cd ~/.openclaw/vault
git init
git add .
git commit -m "Initial commit"
```

### Настройка remote (GitHub)
```bash
# Создай репозиторий на GitHub (без README)
git remote add origin https://github.com/USERNAME/vault.git
git branch -M main
git push -u origin main
```

### Автосинхронизация
```bash
# Добавь в cron (Linux/Mac) или Task Scheduler (Windows)
*/5 * * * * cd ~/.openclaw/vault && git pull && git add . && git commit -m "Auto-sync" && git push
```

## Структура папок
```
vault/
├── People/          # Профили участников
├── Topics/          # Тематические заметки
├── Shared/          # 👥 Совместные файлы
├── Projects/        # Проекты
└── Daily/           # Ежедневные заметки
```

## Workflow
1. Редактируй файлы локально
2. Git автоматически синхронизирует (каждые 5 минут)
3. Уведомления через n8n webhook

## Конфликты
При конфликте Git создаст .conflict файлы. Решай вручную и коммить исправление.
